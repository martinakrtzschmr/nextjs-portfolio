'use client';
import React from 'react';

const AddToCart = () => {
  return (
    <button className="" onClick={() => console.log('add to cart')}>
      Add to cart
    </button>
  );
};

export default AddToCart;
